# These screenshots are referenced from README/dartdoc by their
# raw.githubusercontent URL, so they only need to live in the repository —
# shipping them would just make the published archive heavier.
*
