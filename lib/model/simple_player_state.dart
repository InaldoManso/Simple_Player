/// The SimplePlayerState class represents the state of a player with properties for comfort mode and
/// show time.
class SimplePlayerState {
  bool confortMode;
  String showTime;

  SimplePlayerState({
    required this.confortMode,
    required this.showTime,
  });
}
